<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= genre.name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>    
    <h1><%= genre.name %> </h1>
    <br>
    <p><strong>NAME:</strong>  <%= genre.name %> </p>
    <p><strong>RANKIG:</strong> <%= genre.ranking %> </p>
    <p><strong>PELÍCULAS QUE PERTENECEN A ESTE GÉNERO:</strong></p> 
<ol class="lista_películas">
   <% genre.movies.forEach(movie => { %>
   <li ><a href="/movies/detail/<%= movie.id %>"><%= movie.title %></a></li>
    <% }) %>
   </ol>
</body>
</html>