<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel='stylesheet' href='/css/style.css' />
         <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    </head>
<body class="imagen_deFondo">
    
    <figure class="logo-detail">
        <a href="/"><img src="/img/logo-DH.png" alt="Logo Digital House"></a>
    </figure>
   
        <div class="card">
          <div class="card-header d-flex justify-content-between">
          <div class="card-body">
           <div class="row">
            <div class="col-12 col-md-4">
<img  class="img-fluid" src="/img/<%= movie.image || 'Cine.webp' %>" alt="">
            </div>
            <br>
            <div class="col-12 col-md-8">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item"><b><%= movie.title.toUpperCase() %></b></li>
                    <li class="list-group-item"><b>Género:</b><%= movie.genre ? movie.genre.name : " No especifica el género"%></li>
                    <div class="d-flex justify-content-between">
                        <span>
                            <b>Duración:</b><%= movie.length%>
                        </span>
                        <span>
                           <b>Premios:</b><%= movie.awards %>
                        </span>
                        <span>
                            <b>Rating:</b><%= movie.rating %>
                        </span>
                    </div>
                   </li>
                   <li class="list-group-item">
<!-- fw-bold= para que la letra se vea negrita -->
                    <h5 class="fw-bold">Actores:</h5>
                    <% if (movie.actors.length) { %>
                        <ul class="list-group list-group-flush">
                        <% movie.actors.forEach(actor => { %>
                            <li class="list-group-item"><%= actor.first_name %><%= actor.last_name %> </li>
                        <% }) %>
                        </ul>
                    <% }else{ %>
                       <p class="text-muted">No hay actores vinculados con esta película</p>
                        <% } %>
                   </li>

                    
                </ul>
               
            </div>
    <section >
        <a  class="btn btn-primary " href="/movies/edit/<%= movie.id %>">Editar Pelicula</a>
        <a class="btn btn-primary " href="/movies/delete/<%= movie.id %>">Borrar Pelicula</a>
        <!--Aquí debe colocar los botones de Modificar - Borrar y Listado de Películas-->
    </section>
        </div>


    
  

</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</html>